services:
  api:
    build: ./backend
    ports:
      - "5000:5000"
    volumes:
      - event_data:/app/data

  web:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - api

volumes:
  event_data:


# images  
